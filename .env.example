# Praxis environment variables (placeholder)

# No backend exists yet (Milestone 1 is a static foundation screen only).
# Copy this file to `.env.local` when environment variables are needed:
#
#   cp .env.example .env.local
#
# Future variables (Supabase, auth, AI providers, etc.) will be documented
# here as they are introduced. Do not commit real secrets.
